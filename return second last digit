#include <stdio.h>
int secondlast_digit(int num);
int main() {
    int num;
    printf("%d",secondlast_digit(1429));
    return 0;
}
int secondlast_digit(int num)
{
    if(abs(num)<10)
        return -1;
     else 
        return abs(num % 100)/ 10;
}
